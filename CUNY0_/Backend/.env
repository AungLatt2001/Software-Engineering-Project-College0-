# College0 / CUNY0 — Local Development Configuration
# 
# IMPORTANT: In production, these should be set via environment variables,
# NOT via a .env file (which could be accidentally committed).
# See .env.example for a production template.

COLLEGE0_SECRET=local-dev-secret-change-in-production
COLLEGE0_DB_PATH=college0.db
COLLEGE0_CORS_ORIGINS=*
PORT=5001
FLASK_DEBUG=0
